#include "main.h"
/**
 * _isalpha - check if lowercase or uppercase
 * @c: check if c is a letter
 * Return: 0 or 1
 */
int _isalpha(int c)
{
	int i = _isalpha(c);

	if (i > 0)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
